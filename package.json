{
  "name": "starfleet-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "Starfleet infrastructure visualization monorepo",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r run build",
    "type-check": "pnpm -r run type-check",
    "lint": "pnpm -r run lint"
  },
  "author": "Hyperdrive Technology",
  "license": "MIT",
  "packageManager": "pnpm@8.0.0",
  "engines": {
    "node": ">=18.0.0"
  }
}
